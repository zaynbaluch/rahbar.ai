# app/ — Flutter (Android-only)

On-device runtime: SLM generation (flutter_gemma/LiteRT), query embedding, sqlite-vec retrieval, local SQLite store, Material 3 UI.

Scaffolded with `flutter create` in the build phase. See ../docs/01-architecture.md.
