# pipeline/ — build-time curriculum prep (Python, off-device)

watermark cleanup -> Docling OCR -> block typing -> VLM figure captioning -> SLO-aligned chunking -> EmbeddingGemma -> sqlite-vec curriculum.db

See ../docs/decisions/ADR-005-pdf-preprocessing.md.
